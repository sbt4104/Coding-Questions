#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

const ll MAX_N = 100005;
const LOG=18;
ll m[MAX_N][LOG];
vector<ll> a(MAX_N);

int main() {
    ll n,q,x;
    cin>>n;
    vector<ll> a(n);
    cin>>q;
    while(q--) {
        cin>>x;
    }
    return 0;
}